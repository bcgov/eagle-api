apiVersion: v1
kind: Service
metadata:
  name: {{ include "eagle-api.fullname" . }}
  labels:
    {{- include "eagle-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: 3000-tcp
  selector:
    {{- include "eagle-api.selectorLabels" . | nindent 4 }}
